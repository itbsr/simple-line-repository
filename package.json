{
  "name": "repoline",
  "version": "0.1.0",
  "description": "Parse pasted LINE chat history and store it for later retrieval.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
